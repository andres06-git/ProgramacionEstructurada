#include <stdio.h>
#include <stdlib.h>

/* int Funcion1 (int A)
{
    printf("%d\n", A); // 2. Se imprime de nuevo el valor de 5 ya declarado en el main
    A = 10; // Se cambia el valor de A a 10
    printf("%d\n", A); // 3. Se imprime el valor de A con el nuevo valor (10)
    return 0;
}

int main()
{
    int A = 5;
    printf("%d\n", A); // 1. Imprime el valor de 5 ya declarado
    Funcion1(A); // Se manda a llamar a la funcion
    printf("%d\n", A); // 4. Ya que se mandó a llamar Funcion1, termina ese bloque de código y se imprime el 5 de nuevo
    return 0;
} */

/* void Funcion1 (int *A) // Apuntador -> *
{
    printf("%d\n", *A);
    *A = 10;
    printf("%d\n", *A);
    return 0;
}

int main()
{
    int A = 5;
    printf("%d\n", A);
    Funcion1(&A);
    printf("%d\n", A);
    return 0;
} */

void cuadradoPorValor(int n)
{
    n = n * n;
    printf("[cuadradoPorValor] n = %d\n", n);
}

void cuadradoPorReferencia(int *n)
{
    *n = (*n) * (*n);
    printf("[cuadradoPorReferencia] *n = %d\n", *n);
}

int main()
{
    int a = 3, b = 3;

    printf("Antes de cuadradoPorValor: a = %d\n", a);
    cuadradoPorValor(a);
    printf("Despues de cuadradoPorValor: a = %d\n", a);

    printf("Antes de cuadradoPorReferencia: b = %d\n", b);
    cuadradoPorReferencia(&b);
    printf("Despues de cuadradoPorReferencia: b = %d\n", b);
}
